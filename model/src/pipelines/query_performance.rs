use crate::{Pipeline, StdErr, Topic};

pub fn ask_query_performance_pipelines(_topics: Vec<Topic>) -> Result<Vec<Pipeline>, StdErr> {
    // TODO define all pipeline monitor pipelines
    Ok(Vec::new())
}
